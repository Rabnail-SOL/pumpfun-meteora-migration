/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u64,
} from '@metaplex-foundation/umi/serializers';

export type Global = Account<GlobalAccountData>;

export type GlobalAccountData = {
  discriminator: Uint8Array;
  authority: PublicKey;
  platformFeeRecipient: PublicKey;
  reserve: PublicKey;
  initialVirtualTokenReserves: bigint;
  initialVirtualSolReserves: bigint;
  tokenTotalSupply: bigint;
  platformTradeFeeBps: bigint;
  reserveTradeFeeBps: bigint;
  graduationThreshold: bigint;
};

export type GlobalAccountDataArgs = {
  authority: PublicKey;
  platformFeeRecipient: PublicKey;
  reserve: PublicKey;
  initialVirtualTokenReserves: number | bigint;
  initialVirtualSolReserves: number | bigint;
  tokenTotalSupply: number | bigint;
  platformTradeFeeBps: number | bigint;
  reserveTradeFeeBps: number | bigint;
  graduationThreshold: number | bigint;
};

export function getGlobalAccountDataSerializer(): Serializer<
  GlobalAccountDataArgs,
  GlobalAccountData
> {
  return mapSerializer<GlobalAccountDataArgs, any, GlobalAccountData>(
    struct<GlobalAccountData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['authority', publicKeySerializer()],
        ['platformFeeRecipient', publicKeySerializer()],
        ['reserve', publicKeySerializer()],
        ['initialVirtualTokenReserves', u64()],
        ['initialVirtualSolReserves', u64()],
        ['tokenTotalSupply', u64()],
        ['platformTradeFeeBps', u64()],
        ['reserveTradeFeeBps', u64()],
        ['graduationThreshold', u64()],
      ],
      { description: 'GlobalAccountData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([167, 232, 232, 177, 200, 108, 114, 127]),
    })
  ) as Serializer<GlobalAccountDataArgs, GlobalAccountData>;
}

export function deserializeGlobal(rawAccount: RpcAccount): Global {
  return deserializeAccount(rawAccount, getGlobalAccountDataSerializer());
}

export async function fetchGlobal(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<Global> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'Global');
  return deserializeGlobal(maybeAccount);
}

export async function safeFetchGlobal(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<Global | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists ? deserializeGlobal(maybeAccount) : null;
}

export async function fetchAllGlobal(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<Global[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'Global');
    return deserializeGlobal(maybeAccount);
  });
}

export async function safeFetchAllGlobal(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<Global[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) => deserializeGlobal(maybeAccount as RpcAccount));
}

export function getGlobalGpaBuilder(
  context: Pick<Context, 'rpc' | 'programs'>
) {
  const programId = context.programs.getPublicKey(
    'coinfun',
    '6xHUH1pJ2sLCm8YwyF8DBkUyBjDvoJKWgaXiBLaDdgsp'
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      discriminator: Uint8Array;
      authority: PublicKey;
      platformFeeRecipient: PublicKey;
      reserve: PublicKey;
      initialVirtualTokenReserves: number | bigint;
      initialVirtualSolReserves: number | bigint;
      tokenTotalSupply: number | bigint;
      platformTradeFeeBps: number | bigint;
      reserveTradeFeeBps: number | bigint;
      graduationThreshold: number | bigint;
    }>({
      discriminator: [0, bytes({ size: 8 })],
      authority: [8, publicKeySerializer()],
      platformFeeRecipient: [40, publicKeySerializer()],
      reserve: [72, publicKeySerializer()],
      initialVirtualTokenReserves: [104, u64()],
      initialVirtualSolReserves: [112, u64()],
      tokenTotalSupply: [120, u64()],
      platformTradeFeeBps: [128, u64()],
      reserveTradeFeeBps: [136, u64()],
      graduationThreshold: [144, u64()],
    })
    .deserializeUsing<Global>((account) => deserializeGlobal(account))
    .whereField(
      'discriminator',
      new Uint8Array([167, 232, 232, 177, 200, 108, 114, 127])
    );
}
